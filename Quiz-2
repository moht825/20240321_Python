numbers 리스트의 항목 중 짝수인 것만 필터링해서 result에 저장한 후 출력하세요
---
# 리스트를 정의한다.

numbers = [111, 26, 37, 48]

# filter 함수를 사용하여 짝수만을 걸러내는 람다 함수를 적용 하고 그 결과를 리스트로 변환한다.

result = list(filter(lambda x: x % 2 == 0, numbers))

print(result)
